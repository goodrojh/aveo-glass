# Aveo Glass — лендинг

Next.js 16 · Tailwind 4 · framer-motion. Статический экспорт, деплой на GitHub Pages.

- `npm run dev` — локально (http://localhost:3000)
- `npm run build` — сборка в `out/`

Контент и контакты: `lib/site.ts`. Секции: `components/`.
